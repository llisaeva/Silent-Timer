<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lisaeva.silenttimer">

    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SilentTimer">



        <receiver android:name="SilentAlarmInitiator"  android:exported="true">
            <intent-filter>
                <action android:name="com.lisaeva.silenttimer.START_SILENT_TIMER"/>
                <action android:name="com.lisaeva.silenttimer.STOP_SILENT_TIMER" />
                <action android:name="com.lisaeva.silenttimer.ACTIVATE_SILENT_TIMER" />
                <action android:name="com.lisaeva.silenttimer.DEACTIVATE_SILENT_TIMER" />
            </intent-filter>
        </receiver>

        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>